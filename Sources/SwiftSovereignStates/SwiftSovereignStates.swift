struct SwiftSovereignStates {
    public private(set) var text = "Hello, World!"

    public init() {
        /*for country in Country.allCases {
            let countryBackendID:String = country.rawValue
            if let subdivisions:[any SovereignStateSubdivision] = country.getSubdivisions() {
                for subdivision in subdivisions {
                    print("SwiftSovereignStates.init;countryBackendID=" + countryBackendID + ";subdivision=\(subdivision);shortName=" + subdivision.getShortName() + ";realName=" + (subdivision.getRealName() ?? "nil"))
                }
            }
        }*/
    }
}
